{"id":"long_warm_code_continuation_uncapped","category":"warm_coding","max_tokens":10000,"prompt":"Continue this Python module with production-quality code and no prose. Keep the implementation compact, typed, and deterministic.\n\nfrom __future__ import annotations\n\nimport json\nimport time\nfrom dataclasses import asdict, dataclass\nfrom pathlib import Path\nfrom typing import Any, Iterable\n\n\n@dataclass(frozen=True)\nclass PromptCase:\n    id: str\n    category: str\n    prompt: str\n    max_tokens: int = 128\n    notes: str = \"\"\n\n    @property\n    def prompt_sha256(self) -> str:\n        import hashlib\n        return hashlib.sha256(self.prompt.encode(\"utf-8\")).hexdigest()\n\n\ndef load_prompt_suite(path: Path) -> list[PromptCase]:\n    \"\"\"Load a JSONL prompt suite and validate required fields.\"\"\"\n"}
